trigger:
- main

resources:
- repo: self

variables:
  dockerRegistryServiceConnection: '7db45561-6f48-4c9c-8ced-a2c2999a2309'
  imageRepository: 'ksayeshasiddiqaazuredeveopspipelinewithgithub'
  containerRegistry: 'ksalinuxacr.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      name: 'agent_ayesha'
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
